# Two later change-tasks as an eval

Status: **parked**. High impact on *knowing* whether Goal 0 worked, medium effort. Does not raise first-clone odds.

Origin: [brainstorming/human-input-a.md](../brainstorming/human-input-a.md).

**Why.** Maintainability is “how expensive is the next change.” A first handover does not measure that. Token cost and “did we build what they wanted” are also unmeasured.

**Do.** After one real loop reaches Phase 5, add two follow-on change tasks. Record tokens, whether Phase 2 still holds, and whether headline \(W\) rose. A table, not a framework.

**Depends on.** [finished-tiny-run.md](finished-tiny-run.md) or a live product that actually finished.

**Not.** Calibrating \(W\) on many external codebases first. A JS backend. CI theater.
