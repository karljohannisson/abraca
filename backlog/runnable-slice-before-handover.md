# A runnable slice the human tries before the rest of Phase 4

Status: **parked**. High impact on “meets the need,” medium effort.

**Why.** Phase 4 runs with the human out. Need-meeting is a Phase 5 rewind. Drift accumulates until complaint.

**Do.** After the first few authorities exist, ship one thin vertical the human can run (one screen, one command). Wait for try/accept before the remaining composition tasks. Rewind is still cheap.

**Not.** Putting the human back into site classification or yaml. That stays Phase 4 agent-only for encodings.

**Check.** Orchestrate docs name a mid-Phase-4 try. A cold agent cannot finish every task without a human-accepted slice.
