# Open probes

Resolved probes from the first planning loop are already in `axes.yaml` / Frozen.

## cache (dormant)

What is cached when we add it (exact HTTP response, prompt prefix, tool results)? Until answered, `status: dormant` — no code.
