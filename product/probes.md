# Open probes

Resolved probes from the first planning loop are already in `axes.yaml` / Frozen.
`protocols` was an inferred bet (a new company is not always a new kind of connection) and was accepted.

## cache (dormant)

When we add memory for answers, what is remembered — the exact reply, the start of the prompt, or tool results?
Until answered, `status: dormant` — no code.
